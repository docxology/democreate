# .github/

GitHub-specific configuration for the DemoCreate repository: currently only
[`workflows/ci.yml`](workflows/ci.yml) — a multi-Python CI matrix. No issue/PR
templates or dependabot config exist here (verified by inspection).
