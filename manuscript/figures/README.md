# figures/

Manuscript figures (PNG) plus the generators: `make_figures.py` builds the
figure set (`architecture.png`, `latency.png`, `themes.png`, `video_stills.png`,
`waveform.png`, …) and `graphical_abstract.py` builds the graphical abstract.
`frame_*.png` stills come from rendered demo videos.
