# assets/

Image assets consumed by the example demos: `architecture.png`,
`dashboard.png`, and the `paper/` subtree (figures and page renders backing the
research-paper demo). Built by [`../make_assets.py`](../make_assets.py) from
the canonical sources.
