# tests/assembly/

Tests for the assembly subsystem: audio muxing, caption rendering, the frame compositor (+ extras), and visual effects.

Mirrors `src/democreate/assembly/` — run scoped:
`.venv/bin/python -m pytest -q tests/assembly/`.
