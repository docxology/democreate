# AGENTS.md — .github/

This subtree (inside the democreate repo) is LOCAL-ONLY (matches `.gitignore` rule `projects/*` — never `git add`/commit/push). Repo-wide policy: link up to the template root `AGENTS.md` and `projects/ongoing/AGENTS.md`; do not duplicate it here.