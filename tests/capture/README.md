# tests/capture/

Tests for the capture subsystem: browser capture, screen capture, terminal capture, and deterministic replay.

Mirrors `src/democreate/capture/` — run scoped:
`.venv/bin/python -m pytest -q tests/capture/`.
