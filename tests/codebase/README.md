# tests/codebase/

Tests for the codebase subsystem: repo walking, AST visualization, and dependency analysis (the describe-any-codebase / portfolio path).

Mirrors `src/democreate/codebase/` — run scoped:
`.venv/bin/python -m pytest -q tests/codebase/`.
