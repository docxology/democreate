# tests/paper/

Tests for the paper subsystem: PDF parsing/extraction, structure detection, page/figure handling, and paper-demo narration script generation.

Mirrors `src/democreate/paper/` — run scoped:
`.venv/bin/python -m pytest -q tests/paper/`.
