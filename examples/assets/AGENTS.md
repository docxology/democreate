# AGENTS.md — examples/assets/

- Contents: `architecture.png`, `dashboard.png`, `paper/` (subtree below).
- Regenerate via `examples/make_assets.py`; the demo JSONs reference these
  paths by relative name, so renames break the showcase demos.
