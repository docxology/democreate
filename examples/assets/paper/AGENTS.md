# AGENTS.md — paper/

- `architecture.png`, `lambda_sweep.png` — figure images embedded as demo
  scenes; `pages/` — page renders.
- These mirror figures from the source paper; regenerate alongside
  `examples/make_assets.py` rather than editing in place.
