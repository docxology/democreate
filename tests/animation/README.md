# tests/animation/

Tests for the animation subsystem: diagram slides, text highlights, waveform rendering, zoom/motion, and the guarded Manim adapter (`test_manim.py`).

Mirrors `src/democreate/animation/` — run scoped:
`.venv/bin/python -m pytest -q tests/animation/`.
