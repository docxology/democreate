# Generated outputs are regeneratable; never commit them.
output/
# Flat collection of all per-project summary videos (regeneratable; never public).
project_videos/
.coverage
.coverage.*
coverage.xml
htmlcov/
.pytest_cache/
.ruff_cache/
.mypy_cache/
__pycache__/
*.pyc

# Egg-info from local installs
**/*.egg-info/

# Project-local virtualenv (shared root .venv preferred)
.venv/

# OS
.DS_Store

# Internal Algorithm working doc (PAI-specific); not part of the public package
ISA.md

# Secrets (API keys) — never committed
.env
