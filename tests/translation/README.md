# tests/translation/

Tests for the translation subsystem: the translator backend contract and localization flow.

Mirrors `src/democreate/translation/` — run scoped:
`.venv/bin/python -m pytest -q tests/translation/`.
