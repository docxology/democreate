# AGENTS.md - examples/

Examples are executable documentation for DemoCreate. Keep them deterministic,
small enough for local smoke tests, and aligned with the public CLI/docs.

When an example changes, update `README.md` and rerun the relevant example or
script smoke test before claiming it works.
