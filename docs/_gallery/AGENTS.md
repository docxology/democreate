# AGENTS.md — docs/_gallery/

- All PNGs here are **generated artifacts** of `make_gallery.py`; regenerate,
  don't retouch. If a PNG changes, the generator changed — commit the script.
- `_figure_bg.png` (leading underscore) is a shared background asset consumed
  by other figures, not a standalone gallery image.
- Used by `docs/gallery.md`; keep captions there in sync with regenerated
  images.
